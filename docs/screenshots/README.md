# Screenshots

Dieses Verzeichnis ist für spätere Ergebnis-Screenshots vorgesehen.

Sinnvolle Beispiele:
- SQL Editor: Ergebnis von `SHOW TABLES IN retail_lakehouse;`
- SQL Editor: Row-Count-Validation (`sql/06_validation.sql`)
- SQL Editor: Top 10 aus `gold_product_revenue`
- SQL Editor: Aktuelle Datensätze in `silver_customers_scd2` (`is_current = true`)
